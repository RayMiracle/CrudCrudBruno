meta {
  name: Delete the unicorn
  type: http
  seq: 6
}

delete {
  url: {{BaseURL}}/unicorns/{{unicornId}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
}
